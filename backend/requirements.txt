# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]

# ── Database ──────────────────────────────────────────────────────────────────
SQLAlchemy
psycopg2-binary

# ── Authentication ────────────────────────────────────────────────────────────
python-jose[cryptography]
passlib[bcrypt]
python-multipart

# ── Validation ────────────────────────────────────────────────────────────────
pydantic[email]

# ── Firebase / FCM (Android push + Web Push via Firebase Web SDK) ─────────────
# firebase-admin delivers notifications to:
#   - Android: via FCM
#   - Web/iOS PWA: via FCM web registration tokens (VAPID-based Web Push)
#     iOS Safari 16.4+ is supported when the PWA is installed via Add to Home Screen
firebase-admin

# ── Timezone ──────────────────────────────────────────────────────────────────
pytz

# ── Scheduler ─────────────────────────────────────────────────────────────────
apscheduler

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv
aiofiles