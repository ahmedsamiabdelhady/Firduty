name: Keep API Alive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: |
          curl -L --max-time 30 --fail https://naval-donnamarie-firduty-6e288803.koyeb.app/health